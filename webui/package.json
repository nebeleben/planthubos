{
  "name": "planthub-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/gzip-dist.mjs",
    "test": "node --test src/lib/psc/tests/*.test.mjs"
  },
  "dependencies": {
    "preact": "^10.22.0",
    "uplot": "^1.6.30"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.0",
    "vite": "^5.3.0"
  }
}

/* Filled by Task 4 (MQTT publisher / esp-mqtt client lifecycle). Stub for
 * now so the integrations component compiles with an empty translation
 * unit. */
typedef int keep_translation_unit_nonempty;
